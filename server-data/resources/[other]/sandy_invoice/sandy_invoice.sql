CREATE TABLE `faktury` (
	`praca` VARCHAR(100) NOT NULL,
	`worker` VARCHAR(100) NOT NULL,
	`client` VARCHAR(100) NOT NULL,
	`price` INT NOT NULL,
	`date` VARCHAR(100) NOT NULL,
	`workersteamid` VARCHAR(100) NOT NULL,
	`clientsteamid` VARCHAR(100) NOT NULL,
);
